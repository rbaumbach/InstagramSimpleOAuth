platform :ios, '7.0'

target :InstagramSimpleOAuth do
    pod 'Swizzlean', '0.2.0'
end

target :InstagramSimpleOAuthTests do
    pod 'Specta', '~> 0.2.1'
    pod 'Expecta', '~> 0.2.4'
    pod 'OCMockito', '~> 1.0'
end
